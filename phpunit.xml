<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         executionOrder="depends,defects"
         beStrictAboutOutputDuringTests="true"
         failOnWarning="true"
         failOnRisky="true">
    <testsuites>
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>

    <coverage>
        <include>
            <directory suffix=".php">src</directory>
        </include>
        <report>
            <html outputDirectory="build/coverage/html"/>
            <text outputFile="build/coverage/coverage.txt" showOnlySummary="true"/>
            <clover outputFile="build/coverage/clover.xml"/>
        </report>
    </coverage>

    <php>
        <!-- Per-provider integration credentials. Tests skip when unset. -->
        <!-- Gmail -->
        <!-- <env name="IMAP_GMAIL_HOST" value="imap.gmail.com"/> -->
        <!-- <env name="IMAP_GMAIL_PORT" value="993"/> -->
        <!-- <env name="IMAP_GMAIL_USER" value=""/> -->
        <!-- <env name="IMAP_GMAIL_PASS" value=""/> -->
        <!-- <env name="IMAP_GMAIL_ENCRYPTION" value="tls"/> -->
    </php>
</phpunit>
